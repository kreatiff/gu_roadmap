import * as Icons from 'lucide-react';
console.log(Object.keys(Icons).filter(name => name.toLowerCase().includes('duck')));
console.log(Object.keys(Icons).filter(name => name.toLowerCase().includes('bird')));
console.log(Object.keys(Icons).filter(name => name.toLowerCase().includes('animal')));
